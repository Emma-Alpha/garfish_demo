{
  "name": "garfish-demo",
  "private": true,
  "scripts": {
    "start": "lerna bootstrap && lerna run --parallel start",
    "build": "lerna run build",
    "init": "lerna init"
  },
  "devDependencies": {
    "lerna": "^7.1.1"
  }
}
